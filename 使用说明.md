# Novel Writer Codex 使用说明

本文说明 `0.3.0` 本机 Full-write Beta 的安装与日常使用方法。当前优先支持
Windows Codex Desktop、本地小说项目和本地嵌入模型；公共 Marketplace、GitHub
Release 与完整多平台安装尚未发布。

## 1. 运行要求

- Windows 10/11 与 Codex Desktop。
- Python 3.10 或更高版本。
- Novel Writer Codex 插件源码或已安装的本机插件。
- 本地 `Qwen/Qwen3-Embedding-0.6B` 模型。
- 写章链所需的 Codex 模型权限。context、writer、reviewer、data 固定使用
  `gpt-5.6-luna / high`，不可用时流程会阻断，不会改用主任务模型。

`webnovel_deconstruction_agent` 只在初始化时分析可靠参考文本，并继续继承当前主任务
模型；`$webnovel-plan` 始终由当前主任务模型执行。

## 2. 获取并安装插件

当前版本尚未发布公共 Marketplace。用于本机安装时，建议把仓库克隆到个人插件源码目录：

```powershell
$PluginDir = Join-Path $env:USERPROFILE "plugins\novel-writer-codex"
git clone https://github.com/Killuazyt/novel-writer-codex.git $PluginDir
```

若目录已经存在，正常使用 Git 拉取更新，不要直接覆盖其中的用户改动。然后在 Codex 中
使用内置 `$plugin-creator` 将该目录加入默认 personal marketplace，并从 Codex App 的
本地插件页面安装 `novel-writer-codex`。如果本机 Codex CLI 可用，也可以从已配置的
personal marketplace 安装：

```powershell
codex plugin add novel-writer-codex@personal
```

安装或更新插件后必须打开一个新的 Codex 任务，旧任务不会自动重新加载新版 Skill。

## 3. 安装 Python 与本地 RAG 依赖

在插件源码目录运行：

```powershell
python -m pip install -r requirements.txt
```

如果只需补本地嵌入依赖：

```powershell
python -m pip install -U "sentence-transformers>=2.7.0" "transformers>=4.51.0" "huggingface-hub>=0.28.0"
```

## 4. 下载本地嵌入模型

默认模型只需下载一次，所有小说项目共同使用，不需要 `EMBED_API_KEY`。

### Hugging Face

```powershell
$ModelDir = Join-Path $env:USERPROFILE ".codex\novel-writer-codex\models\Qwen3-Embedding-0.6B"
hf download Qwen/Qwen3-Embedding-0.6B --local-dir $ModelDir
```

### ModelScope 备用下载方式

```powershell
python -m pip install -U modelscope
$ModelDir = Join-Path $env:USERPROFILE ".codex\novel-writer-codex\models\Qwen3-Embedding-0.6B"
modelscope download --model Qwen/Qwen3-Embedding-0.6B --local_dir $ModelDir
```

runtime 始终使用 `local_files_only` 加载模型，不会在写章或查询时自动联网下载。

新项目生成的 `.env.example` 已使用本地默认配置：

```dotenv
EMBED_API_TYPE=local
EMBED_MODEL=Qwen/Qwen3-Embedding-0.6B
EMBED_MODEL_PATH=
EMBED_DEVICE=auto
EMBED_BATCH_SIZE=8
EMBED_NORMALIZE=true
RERANK_API_TYPE=disabled
```

模型位于默认目录时，不需要在提示词或项目配置中填写路径。只有模型放在其他位置时，
才把 `.env.example` 复制为 `.env`，并填写绝对路径：

```dotenv
EMBED_MODEL_PATH=F:\models\Qwen3-Embedding-0.6B
```

## 5. 初始化一本新书

Init 要求小说根目录是工作区根目录的直接子目录。例如：

```text
工作区根目录：F:\codexnovel\books
项目文件夹名：my-novel
最终小说根目录：F:\codexnovel\books\my-novel
```

先在 Codex 中打开父目录 `F:\codexnovel\books`，然后使用以下提示词：

```text
请使用 $webnovel-init 初始化一部全新中文网文项目。

工作区根目录：F:\codexnovel\books
项目文件夹名：<安全的单层文件夹名>
Git 模式：off
构思来源：仅原创，不使用参考作品，不联网。

书名：<书名>
题材：<题材>
目标规模：<预计字数或章节数>
一句话故事：<一句话概括>
核心冲突：<核心冲突>
主角姓名：<姓名>
主角欲望：<核心目标>
主角缺陷：<会产生真实后果的缺陷>
关系结构：<单主角、双主角或群像>
金手指：<类型和名称；没有则明确写无金手指>
不可逆代价：<代价或不适用原因>
世界规模：<城市、国家、多世界等>
力量体系：<类型>
主要势力：<势力>
社会阶层：<阶层结构>
资源分配：<稀缺资源和控制方式>
反派镜像：<反派如何映照主角>
反套路：<希望避开的常见套路>
开篇钩子：<第一章核心钩子>
硬约束：
1. <约束一>
2. <约束二>

使用默认本地 Qwen/Qwen3-Embedding-0.6B，不要求 EMBED_API_KEY，
不调用云端 embedding 或 rerank。若信息不足，请分批向我提问。
资料完整后先执行零写入 dry-run，展示最终路径、创建清单、Git 模式和
Apply/Revise/Cancel 选择，然后停下等待我的真实回答。不得替我选择 Apply。
```

核对预览无误后回复：

```text
Apply
```

Init 默认 `git-mode off`。只有明确选择 `init` 或 `initial-commit` 时才会创建 Git
状态；初始化一本小说不等于授权 push、tag 或发布。

## 6. 安装项目 Agent

初始化成功后，把生成的小说根目录作为 Codex 工作区打开，并发送：

```text
请使用 $webnovel-setup 检查并配置当前工作区的 5 个项目 Agent。
若需要变更，列出 created、updated、unchanged，并停下等待我回复 Apply。
```

查看变更清单后回复 `Apply`。Setup 应再次检查为 `5/5 current`，然后必须打开新的
Codex 任务。Agent 合同升级时也执行同一流程；旧的 `medium` 配置会被更新为当前
`gpt-5.6-luna / high`。

## 7. 规划、检查与写章

### 规划第一卷

```text
Setup 已完成并复查 5/5 current。请使用 $webnovel-plan 规划第1卷。
完成合同、apply 和 prewrite 校验后运行 $webnovel-doctor。
遇到任何作者裁决都停下等待我选择，不得代选。
```

### 写第一章

```text
请使用 $webnovel-write default 明确写第1章。
使用托管的真实 gpt-5.6-luna / high 子 Agent，完成 context、draft、独立 review、
polish、data、precommit、chapter-commit、五项投影和 postcommit。
不得由主任务代写、伪造 Agent evidence 或绕过 blocker。
```

写下一章时只需替换章节号。正常情况下使用 `default`；`fast` 和 `minimal` 会缩减审查，
但不会跳过事务、数据或提交 gate。

## 8. 常用功能

| 目的 | 提示词示例 |
|---|---|
| 健康检查 | `请使用 $webnovel-doctor 检查当前小说项目。` |
| 查询设定 | `请使用 $webnovel-query 查询林砚的当前状态和相关伏笔。` |
| 单章审查 | `请使用 $webnovel-review 对第1章执行 full Review。` |
| 记录写法 | `请使用 $webnovel-learn 记住第1章这种开篇钩子写法。` |
| 查看进度 | `请使用 $webnovel-dashboard 启动只读本地 Dashboard。` |

Dashboard 只绑定 `127.0.0.1`，默认不自动打开浏览器。停止时发送：

```text
请使用 $webnovel-dashboard 停止当前项目的 Dashboard。
```

## 9. 更新插件

更新本地插件源码后：

1. 通过 `$plugin-creator` 更新本机插件 cachebuster。
2. 从 personal marketplace 重新安装插件。
3. 打开新 Codex 任务。
4. 在每个小说工作区运行 `$webnovel-setup`。
5. 若显示 Agent 合同过期，查看清单后回复 `Apply`，再打开新任务。

不要手工编辑 `.codex/agents/*.toml`；这些文件由 Setup 根据插件内的规范合同生成和
校验。

## 10. 常见问题

### Doctor 报告本地模型缺失

确认默认模型目录中存在 `config.json`、tokenizer 文件和 `model.safetensors`。模型放在
其他目录时，在项目 `.env` 中设置绝对 `EMBED_MODEL_PATH`，然后重新运行
`$webnovel-doctor`。

### Agent 显示 unavailable 或 contract hash mismatch

运行 `$webnovel-setup`，确认变更后回复 `Apply`，并打开新任务。不要让主任务模拟缺失的
Agent。

### Projection 失败

只恢复失败的 projection，不要重复生成正文或重复执行 `chapter-commit`。优先让
`$webnovel-write` 从现有 run 恢复；诊断时先运行 `$webnovel-doctor`。

### 是否需要每个项目重新下载模型

不需要。默认模型保存在用户级 `WEBNOVEL_HOME` 下，所有小说项目共享同一份本地模型。

### 是否必须使用 Git

不需要。Init 默认关闭 Git，非 Git 小说项目可以正常完成写章、提交事实和投影。
